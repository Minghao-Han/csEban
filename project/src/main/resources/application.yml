server:
  port: 8080

spring:
  mvc:
    static-path-pattern: /static/**
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:mysql://localhost:3306/cseban?serverTimezone=Asia/Shanghai&characterEncoding=utf-8&useUnicode=true&allowMultiQueries=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  type-aliases-package: com.AAA.csEban.pojo
  mapper-locations: classpath:mybatis/*.xml
  config-location: classpath:/mybatis-config.xml

